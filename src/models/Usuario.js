class Usuario {
    constructor(nome, login, senha, token, id, ) {
        this.id = id;
        this.nome = nome;
        this.login = login;
        this.senha = senha;
        this.token = token;
    }
}

export default Usuario;